# Bullet Chess Tracker — Design

**Date:** 2026-05-26
**Owner:** M_V-V (madisonveldingvandam.artist@gmail.com)
**Status:** Draft for review

## Purpose

A local, single-user dashboard that turns a Chess.com bullet history into a *ranked repertoire view* and an *actionable metrics panel*. Built to answer two questions every refresh:

1. **Which openings should I keep playing, drop, or experiment with next?**
2. **What process metric is leaking rating right now (time mgmt, tilt, opening velocity, etc.)?**

Out of scope for v1: engine analysis, social/sharing features, mobile UI, formats other than bullet.

## Constraints

- macOS, Python 3.14 + Node already installed
- Chess.com public API (no auth required for public games)
- Single user, single machine, runs offline after data refresh
- No persistent server — refresh is an explicit script run

## High-level architecture

```
┌──────────────────────────────────────────────────────────┐
│                    refresh.py (CLI)                       │
│  pulls Chess.com archives  →  computes metrics  →         │
│  merges with annotations.json  →  writes index.html       │
└──────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────┐
│             index.html (open in browser)                  │
│  Tabulator.js tables, vanilla JS, inline SVG sparklines   │
│  Edits to annotations write back via download-or-copy     │
└──────────────────────────────────────────────────────────┘
```

Two components only: a Python pipeline (`refresh.py`) and a static HTML dashboard (`index.html`). They communicate via JSON files on disk.

### Why this shape

- **No server** — the user opens an HTML file. No port, no process to keep alive.
- **Static output** — re-running the refresh script regenerates the page in place.
- **Annotations as JSON** — human-readable, git-trackable, easy to back up.

## File layout

```
chess-tracker/
├── refresh.py              # the pipeline (CLI entrypoint)
├── chess_tracker/          # python package
│   ├── __init__.py
│   ├── api.py              # Chess.com API client (stdlib urllib)
│   ├── pgn.py              # PGN + clock parsing
│   ├── metrics.py          # all metric computations
│   ├── render.py           # HTML rendering (template + JSON injection)
│   └── annotations.py      # read/write annotations.json
├── data/
│   ├── raw/                # cached monthly archive JSON from Chess.com
│   ├── computed.json       # all derived metrics for the dashboard
│   └── annotations.json    # user-written tags, notes, error log
├── dashboard/
│   ├── index.html          # the dashboard (regenerated by refresh.py)
│   ├── app.js              # Tabulator setup, interactions
│   └── styles.css
├── docs/superpowers/specs/ # this spec lives here
└── README.md
```

## Data model

### `data/raw/{YYYY-MM}.json`

Raw monthly archives from Chess.com, cached verbatim. Refresh policy: the current calendar month is always re-fetched (games are added continuously); past months are fetched only the first time they appear in the archives index. Manual `--force` flag can re-fetch everything.

### `data/computed.json`

Single JSON object produced fresh on every refresh. Top-level shape:

```json
{
  "generated_at": "2026-05-26T16:07:00Z",
  "username": "M_V-V",
  "format": "bullet",
  "kpis": { ... },
  "repertoire": [ { opening_row }, ... ],
  "conditions": { "hour_of_day": [...], "session_position": [...], "opp_rating_bucket": [...] },
  "sessions": [ { session_row }, ... ],
  "games": [ { game_row }, ... ]
}
```

### `data/annotations.json`

User-owned, never overwritten by the pipeline. Schema:

```json
{
  "openings": {
    "Queens Pawn Opening Zukertort Chigorin Variation": {
      "tag": "in_repertoire",  // in_repertoire | experimenting | drop
      "note": "main d4 weapon",
      "plans": ["c3 + Nbd2", "kingside expansion with h3-Bh2"]
    }
  },
  "games": {
    "<game_url>": {
      "tags": ["hung-knight", "endgame"],
      "note": "I keep playing Nxe5 here without checking f7"
    }
  },
  "error_log": [
    {
      "id": "err-001",
      "created": "2026-05-26",
      "title": "Blunder queen to back-rank in time scramble",
      "pattern": "When under 5s I move my queen carelessly",
      "game_refs": ["<game_url>", "<game_url>"]
    }
  ]
}
```

## Metrics catalog (the heart of the spec)

All computed from PGN + clock annotations. No engine.

### KPI strip (top of dashboard)

| Metric | Formula |
|---|---|
| Current rating | last game's `me.rating` |
| 7-day Δ | rating_now – rating_7d_ago |
| Games today | count(games with end_time today) |
| Win% today | wins_today / games_today |
| Tilt status | 🟢 if win% of last 5 ≥ 60%, 🟡 if 40 ≤ win% < 60, 🔴 if win% < 40 |

### Repertoire table (rows = opening families)

Opening family = ECO opening name with trailing move-number tokens stripped (e.g., `Queens Pawn Opening Zukertort Chigorin Variation 3.Bf4` → `Queens Pawn Opening Zukertort Chigorin Variation`).

| Column | Definition |
|---|---|
| Opening | Family name |
| ECO | Most common ECO code in this family |
| Color | white / black |
| N | Games played |
| Win% | wins / N |
| Flag% | timeout-losses / losses |
| Mate% | checkmated-losses / losses |
| MedLen | median full-moves in this opening |
| Form | sparkline of last 10 results (W=up, L=down) |
| AvgOppRating | mean opponent rating |
| Δ-yours | mean(my_rating – opp_rating) |
| Tag | from annotations: 📌 / 🧪 / 🗑 |

Conditional formatting: Win% cell green ≥60%, red ≤35%. Flag%/Mate% red if ≥60% (signals leak).

### Conditions table (rows = buckets)

Three sub-tables, each with `Bucket · N · Win% · Mate% · Flag%`:

- **Hour-of-day** (00–23, local time)
- **Session position** (game 1–5, 6–10, 11–20, 21+)
- **Opponent rating delta** (5 buckets, with respect to your current rating: < –150, –150 to –50, –50 to +50, +50 to +150, > +150)

### Sessions table (rows = sessions)

Session = consecutive games with no 10-min gap.

| Column | Definition |
|---|---|
| Start | local datetime |
| Games | count |
| Duration | minutes |
| Rating Δ | end_rating – start_rating |
| W-L-D | tuple |
| Tilt flag | 🔴 if Δ ≤ –50 |

### Error log table (rows = entries)

Reads `annotations.json.error_log`. Columns: Title · Pattern · # of linked games · Created. Clicking expands to show linked game URLs.

## The four-table layout

Single-page scroll, in this order:

1. **KPI strip** (sticky on scroll)
2. **Repertoire** (tabs: White / Black / All)
3. **Conditions** (3 sub-tables side-by-side)
4. **Sessions** (latest first)
5. **Error log** (last)

All tables use Tabulator.js: sortable by any column, filterable via header inputs, exportable to CSV.

## Refresh flow

```
$ python3 refresh.py
[1/5] Loading archives index from chess.com... 12 archives
[2/5] Refreshing 2026-05 (had 523 games, fetched 527)... 4 new
[3/5] Parsing PGNs + clocks... 527 bullet games
[4/5] Computing metrics... done
[5/5] Merging annotations + rendering dashboard → dashboard/index.html

Open: file:///Users/madisonvelding-vandam/Developer/chess-tracker/dashboard/index.html
```

Idempotent. Cached archives skip re-fetch unless current-month.

## Annotation editing flow

**v1: manual JSON edits.** `data/annotations.json` is human-readable and the schema is small. User edits the file directly with their text editor of choice. Re-running `refresh.py` picks up changes immediately.

**v1.1 (deferred):** browser-side modal that constructs an updated `annotations.json` and triggers a download. User drops the file back into `data/`. Zero-infrastructure but adds modal+download JS.

**Rejected for both:** tiny local Flask server for write-back. Adds a process to manage and breaks the static-HTML guarantee.

## Technology choices

| Layer | Pick | Why |
|---|---|---|
| Pipeline | Python 3.14 stdlib only | No deps, fewer install steps |
| HTTP | urllib | Stdlib; rate is low |
| HTML rendering | Python f-string templates | Simple, no Jinja dep |
| Tables | Tabulator.js (CDN) | Modern, sortable/filterable/sparklines, MIT |
| Charts | Inline SVG sparklines | No chart lib needed for v1 |
| Styles | Vanilla CSS, dark theme | Matches user's chess.com theme |

## Testing

- `pgn.py` and `metrics.py` are pure functions. Unit tests via `pytest` (added as dev dep).
- Fixtures: a few real PGNs cached as test data.
- API client tested with a mocked `urlopen`.
- Render is verified by snapshot comparison (HTML output diff).

## What's deliberately NOT in v1

- Engine analysis / ACPL / CAPS (Tier 2 in the brainstorm). Requires running Stockfish or scraping Chess.com Game Review. Future hook: a `engine.py` module that reads Game Review HTML.
- Daily / Rapid formats. Data model is format-tagged so adding them later = new tab + filter, no refactor.
- Live updates / web sockets / push notifications.
- Multi-user / accounts.
- Opening trainer / drill mode.

## Open questions for the reviewer (you)

None — all key choices have been settled in brainstorming. If anything in this doc surprises you, flag it and we'll revise before writing the implementation plan.
